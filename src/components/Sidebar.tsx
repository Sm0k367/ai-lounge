import React, { useState } from 'react';\nimport { Button } from './ui/button';\nimport { MessageSquare, Image, Code, Mic, Moon, Sun } from 'lucide-react';\n\ntype Tab = 'chat' | 'images' | 'code' | 'voice';\n\ninterface SidebarProps {\n  activeTab: Tab;\n  onTabChange: (tab: Tab) => void;\n  onThemeToggle: () => void;\n  isDark: boolean;\n}\n\nconst Sidebar: React.FC<SidebarProps> = ({ activeTab, onTabChange, onThemeToggle, isDark }) => {\n  const tabs: { id: Tab; label: string; icon: React.ReactNode }[] = [\n    { id: 'chat', label: 'Chat', icon: <MessageSquare className=\"w-5 h-5\" /> },\n    { id: 'images', label: 'Images', icon: <Image className=\"w-5 h-5\" /> },\n    { id: 'code', label: 'Code', icon: <Code className=\"w-5 h-5\" /> },\n    { id: 'voice', label: 'Voice', icon: <Mic className=\"w-5 h-5\" /> },\n  ];\n\n  return (\n    <div className={`w-64 h-screen bg-black/80 backdrop-blur-xl border-r border-purple-500/40 flex flex-col p-6 space-y-4 neon-glow ${isDark ? 'bg-black/90' : 'bg-gray-900/90'}`}>\n      <div className=\"flex items-center justify-between mb-8\">\n        <h2 className=\"text-2xl font-bold bg-gradient-to-r from-purple-400 to-pink-400 bg-clip-text text-transparent glitch-text animate-glow\">\n          AI Lounge\n        </h2>\n        <Button\n          variant=\"ghost\"\n          size=\"icon\"\n          onClick={onThemeToggle}\n          className=\"w-10 h-10 p-0 hover:bg-purple-500/20\"\n        >\n          {isDark ? <Sun className=\"w-5 h-5\" /> : <Moon className=\"w-5 h-5\" />}\n        </Button>\n      </div>\n      <nav className=\"flex-1 space-y-2\">\n        {tabs.map((tab) => (\n          <Button\n            key={tab.id}\n            variant={activeTab === tab.id ? 'secondary' : 'ghost'}\n            className={`w-full justify-start h-14 neon-glow hover:bg-purple-500/20 ${activeTab === tab.id ? 'bg-purple-500/30 border-purple-400/50' : ''}`}\n            onClick={() => onTabChange(tab.id)}\n          >\n            <span className=\"mr-3 opacity-90\">{tab.icon}</span>\n            {tab.label}\n          </Button>\n        ))}\n      </nav>\n      <div className=\"pt-4 border-t border-purple-500/30\">\n        <Button variant=\"outline\" className=\"w-full neon-glow\">\n          New Session\n        </Button>\n      </div>\n    </div>\n  );\n};\n\nexport default Sidebar;\n